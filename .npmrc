# Hoist all packages to root node_modules (npm-style flat layout).
# Required because pnpm's strict isolation prevents @auth/core from
# resolving its 'nodemailer' optional peer dependency during Next.js
# webpack bundling on Railway/nixpacks.
shamefully-hoist=true
